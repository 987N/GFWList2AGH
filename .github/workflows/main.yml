# Current Version: 1.0.4

name: Build & Release 987N/GFWList2AGH

on:
    push:
        branches: [main]
    schedule:
        - cron: "0 0 * * *"
    workflow_dispatch:  # ✅ 允许手动执行

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Step 1 - Checkout
              uses: actions/checkout@v4
            - name: Step 2 - Build GFWList2AGH
              run: |
                  curl -s "https://raw.githubusercontent.com/987N/GFWList2AGH/source/release.sh" | sudo bash
            - name: Step 3 - Release GFWList2AGH
              run: |
                  curl -s "https://raw.githubusercontent.com/WPF0414/Toolkit/main/Git.sh" > "/tmp/Git.sh"
                  sudo bash "/tmp/Git.sh" -u "987N" -e "lcl253908634@gmail.com" -f "." -r "GFWList2AGH" -i "Generated by GitHub Actions" -m "push"
